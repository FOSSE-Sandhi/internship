<?xml version="1.0"?>
<block>
  <name>bode</name>
  <key>bode_bode</key>
  <category>bode</category>
  <import>import bode</import>
  <make>bode.bode()
self.$(id).set_parameters($a,$b,$c,$d)
</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Transfer function 1 numerator</name>
    <key>a</key>
    <value></value>
    <type>string</type>
  </param>
  <param>
    <name>Transfer function 1 denominator</name>
    <key>b</key>
    <value></value>
    <type>string</type>
  </param>
  <param>
    <name>Transfer function 2 numerator</name>
    <key>c</key>
    <value></value>
    <type>string</type>
  </param>
  <param>
    <name>Transfer function 2 denominator</name>
    <key>d</key>
    <value></value>
    <type>string</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>float</type>
  </source>
</block>
